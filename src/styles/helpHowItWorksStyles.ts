import { StyleSheet } from 'react-native';

import { theme } from '@/styles/theme';

export const helpHowItWorksStyles = StyleSheet.create({
    scrollView: {
        backgroundColor: theme.colors.background.primary,
    },
});
